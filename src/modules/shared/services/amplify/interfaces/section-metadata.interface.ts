export interface SectionMetadata {
    id: string;
    name: string;
    publisherId: string;
    publisherName: string;
    url: string;
    publisherUrl: string;
    isOen: boolean; //Api meant isOpen? typo in API?
}
